import * as React from "react";
import { SectionTitle } from "figma-styled-components";

export default function EnvironmentPanel() {
  return (
    <div className="panel">
      {/* section1 */}
      <div className="ui-section">
        <SectionTitle className="divider">Lighting</SectionTitle>
      </div>
    </div>
  );
}
